<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <script src="/js/header.js" defer></script>

    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <%- include("../partials/header") %>
    
    <div class="scroll-container">
    <div class="scroll-list">
        <div class="scroll-group">
        <% images.forEach(image => { %>
            <div class="tag"><img src="<%= image.url %>" alt="<%= image.description %>"></div>
        <% }) %>
        </div>
        <div class="scroll-group" aria-hidden="true">
        <% images.forEach(image => { %>
            <div class="tag"><img src="<%= image.url %>" alt="<%= image.description %>"></div>
        <% }) %>
        </div>
    </div>

        <div class="scroll-text">
            Cascadia Gear Co-op
        </div>

        <div class="fade-left"></div>
        <div class="fade-right"></div>
    </div>

    <main class="container">
        <section class="mission section">
            <div class="text align-items-center">
                <h1>Welcome to Cascadia Gear Co-op</h1>
                <p>Your one-stop shop for all your outdoor gear needs. Explore our wide selection of camping, hiking, and adventure equipment.</p>
            </div>
            <div class="image-placeholder"><img src="https://images.pexels.com/photos/714258/pexels-photo-714258.jpeg?" alt="Mountains with whitecap snow"/></div>
        </section>

        <section class="focus section">
            <div class="text align-items-center">
                <h2>Our Focus</h2>
                <ul>
                    <li>Quality outdoor gear at affordable prices</li>
                    <li>Exceptional customer service</li>
                    <li>Community-driven co-op model</li>
                </ul>
            </div>
            <div class="image-placeholder"><img src="https://images.pexels.com/photos/443446/pexels-photo-443446.jpeg" alt="Outdoor Gear"/></div>
        </section>

        <section class="getStarted section center">
            <div class="text">
                <h2>Get Started</h2>
                <p>Browse our products, create an account, and start your outdoor adventure today!</p>
                <p class="note">When authentication is implemented, our product page will be behind a login wall.</p>
                <a href="/register" class="cta-button">Create Account</a>
                <a href="/products" class="cta-button">Browse Products</a>
            </div>
        </section>

        <section class="spotlight section">
            <% if (featuredProduct) { %>
                <div class="text spotlight-text">
                    <h2>Spotlight Product</h2>
                    <h3 class="spotlight-name"><%= featuredProduct.name %></h3>
                    <p><%= featuredProduct.description %></p>
                    <p class="spotlight-price">$<%= Number(featuredProduct.price).toFixed(2) %></p>
                    <% if (featuredProduct.category_name) { %>
                        <p class="spotlight-category"><%= featuredProduct.category_name %></p>
                    <% } %>
                </div>
                <div class="spotlight-media">
                    <img
                        src="<%= featuredProduct.image_url || '/images/default.webp' %>"
                        alt="<%= featuredProduct.name %>"
                        class="spotlight-image"
                    />
                </div>
            <% } else { %>
                <div class="text align-items-center">
                    <h2>Spotlight Product</h2>
                    <p>Check out our featured product of the month! This will showcase one of our top-selling items.</p>
                </div>
                <div class="image-placeholder large"></div>
            <% } %>
        </section>
    </main>
    

    <%- include("../partials/footer") %>
</body>
</html>
